# Copy to .env and fill the key for the provider selected in config.yaml.
# .env is gitignored. Existing environment variables take precedence.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
# AWS_PROFILE=your-profile
# AWS_DEFAULT_REGION=your-region
